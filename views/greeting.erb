<!DOCTYPE html>
<html>
<head>
  <style>
    .center {
      text-align: center;
      color: mediumblue;
    }
    table {
      border-collapse: collapse;
      width: 100%;
    }
    th, td {
      padding: 8px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }
    tr:hover{background-color:#f5f5f5}
    img {
      display: block;
      margin: 0 auto;
    }
  </style>
</head>
<body>

<h1 class="center"><%= "You can obtain the following info from the admin/me endpoint" %></h1>
<table>
  <tr>
    <th>Attribute</th>
    <th>Value</th>
    <th>Type</th>
    <th>Description</th>
  </tr>
  <tr>
    <td>type</td>
    <td><%= @type %></td>
    <td>string</td>
    <td>value is 'admin'</td>
  </tr>
  <tr>
    <td>id</td>
    <td><%= @id %></td>
    <td>string</td>
    <td>The id of the admin</td>
  </tr>
  <tr>
    <td>name</td>
    <td><%= @name %></td>
    <td>string</td>
    <td>The name of the admin</td>
  </tr>
  <tr>
    <td>email</td>
    <td><%= @email %></td>
    <td>string</td>
    <td>The email address of the admin</td>
  </tr>
  <tr>
    <td>app.type</td>
    <td><%= @app_type %></td>
    <td>string</td>
    <td>value is 'app'</td>
  </tr>
  <tr>
    <td>app.id_code</td>
    <td><%= @app_code %></td>
    <td>string</td>
    <td>The id of the app</td>
  </tr>
  <tr>
    <td>app.created_at</td>
    <td><%= @app_create_date %></td>
    <td>timestamd</td>
    <td>When the app was created</td>
  </tr>
  <tr>
    <td>app.secure</td>
    <td><%= @app_secure_mode %></td>
    <td>boolean</td>
    <td>Whether or not the app uses Secure Mode</td>
  </tr>
</table>

<h2 class="center">Intercom Avatar Image</h2>
<img src=<%= @avatar %> alt="Avatar" style="width:10%">


<h1 class="center"><%= "Counts endpoint via the Ruby library" %></h1>
<table>
  <tr>
    <th>Count Type</th>
    <th>Value</th>
  </tr>
  <tr>
    <td>Users</td>
    <td><%= @counts.user["count"] %></td>
  </tr>
  <tr>
    <td>Leads</td>
    <td><%= @counts.lead["count"] %></td>
  </tr>
  </tr>
</table>

</body>
</html>
